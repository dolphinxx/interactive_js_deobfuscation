import {runTransformerTest} from "./test-util";
import {hexadecimal} from "../src/transform";

describe('Hexadecimal', function () {
    it('hexadecimal', () => {
        runTransformerTest('hexadecimal', []);
    });
});
